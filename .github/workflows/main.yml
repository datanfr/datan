name: Server Deploy 
on: push
jobs: 
  web-deploy: 
    name: Deploy Website 
    runs-on: ubuntu-latest
    steps: 
      - name: deploy
        uses: appleboy/ssh-action@v1.2.0
        with: 
          host: 
          username:
          passowrd:
          port: 22
          script:
            cd public_html 
            git pull origin master         


